import { DisbursementValidateDirective } from './disbursement-validate.directive';

describe('DisbursementValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new DisbursementValidateDirective();
    expect(directive).toBeTruthy();
  });
});
